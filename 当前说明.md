# 当前说明

## 1. 文档定位

本文档用于说明 `因果推断` 项目的当前状态、代码结构、各模块职责、各模块目前存在的问题，以及后续最值得优先处理的方向。

截至 `2026-07-21`，本地仓库当前主分支 `main` 已经回到提交 `71f8546`，这是一版“曾经成功跑通 10 条 GSM8K 全流程”的稳定基线。这里的“跑通”指的是：

1. 能准备数据。
2. 能导出监督数据。
3. 能训练本地模型。
4. 能跑预测。
5. 能做评测。

这里的“跑通”不等于“方法已经被实验验证有效”。从目前已经做过的实验来看，工程链路能走完，但 TTL 方法本身还没有稳定表现出优于基座模型的收益。

---

## 2. 先给结论

### 2.1 当前项目已经做到的事情

1. 已经把 `CloudDeviceTTL` 中最关键的“因果协作 TTL”思路拆出来，重构成一个独立工程。
2. 已经具备本地可运行的 `数据准备 -> 监督导出 -> 训练 -> 推理 -> 评测` 闭环。
3. 已经支持用 `OpenAI-compatible API` 作为 teacher，支持把监督数据导出成 `answer / counterfactual / policy` 三类样本。
4. 已经把 PPT 里三阶段方法中的核心概念映射到代码结构里：
   - Stage 1：多路径推理与聚类
   - Stage 2：诊断与正确答案生成
   - Stage 3：基于干预收益的 TTL 风格训练信号
5. 已经支持 GSM8K 风格数据的实验，并且做过 `10` 条规模的完整跑通。

### 2.2 当前项目还没有做到的事情

1. 还没有真正接入“真实的边侧 SLM 多路径生成器”。
2. 还没有真正接入“稳定可靠的云侧 LLM 诊断器”。
3. 还没有证明当前 TTL 训练后的模型在 GSM8K 上稳定优于基座模型。
4. 还没有把推理阶段的“ASK_CLOUD 交互”稳定地跑起来。
5. 还没有形成自动化测试、断点续跑、缓存治理、错误恢复这些成熟工程能力。

### 2.3 当前最核心的问题

1. teacher API 不稳定，既出过环境变量编码问题，也出过鉴权失败问题。
2. teacher 输出的结构化 JSON 质量不稳定，所以解析模块做得很复杂，但仍然会有空字段、半截 JSON、JSON 混 prose 的情况。
3. 当前 `v7` 推理提示词过于简单，推理时没有稳定触发真正的云调用，因此 `avg_cloud_calls` 多次为 `0.0`。
4. frontdoor 流程目前仍然主要是 mock / heuristic 版，更像“离线加权与方法占位”，还不是“真实部署链路”。
5. 仓库里同时存在两条主线：
   - 一条是旧的 mock / dry-run / demo 线
   - 一条是现在真正用于 GSM8K 和 CloudDeviceTTL 迁移的 export / train / predict / evaluate 线
   这会让新读代码的人很容易混淆。

---

## 3. 整体架构

### 3.1 当前仓库其实有两套系统

#### 第一套：mock 参考实现

这一套主要是为了把 PPT 方法跑成一个可解释、可观察、可导出 trace 的“参考版”。

核心入口：

- `run_demo.py`
- `experiment_runner.py`
- `baselines.py`
- `metrics.py`
- `causal_ttl/frontdoor/*`

这一套的特点：

1. 它不依赖真实 SLM。
2. 它不依赖真实 teacher API。
3. 它能把 `多路径 -> 聚类 -> 诊断 -> 干预` 的前门流程表达清楚。
4. 它更适合演示方法和导出 frontdoor 权重，不适合直接拿来做最终实验结论。

#### 第二套：当前主实验链路

这一套是现在真正用于导出监督、训练本地模型、做 GSM8K 实验的主线。

核心入口：

- `prepare_dataset.py`
- `prepare_gsm8k_subset.py`
- `export_supervision.py`
- `train_causal_ttl.py`
- `predict_causal_ttl.py`
- `evaluate_causal_ttl.py`
- `run_causal_workflow.py`

这一套的特点：

1. 已经能本地闭环。
2. 已经支持 teacher API。
3. 已经支持 LoRA 训练。
4. 已经支持 GSM8K 数值评测。
5. 当前真正的问题主要集中在 teacher 稳定性和 v7 推理交互有效性上。

### 3.2 端到端流程

当前主流程可以按下面的顺序理解：

1. `prepare_dataset.py / prepare_gsm8k_subset.py`
   - 把原始数据整理成统一的 `train.json / valid.json / test.json`。
2. `export_supervision.py`
   - 调 teacher 生成结构化因果 payload。
   - 可选调用 frontdoor mock 流水线计算样本权重。
   - 导出 `answer / counterfactual / policy` 监督数据。
3. `train_causal_ttl.py`
   - 对本地模型做 LoRA 或 full fine-tuning。
4. `predict_causal_ttl.py`
   - 读取测试集。
   - 构造推理 prompt。
   - 在 `fact / v6 / v7` 模式下生成答案。
5. `evaluate_causal_ttl.py`
   - 对输出 JSONL 做 text 或 GSM8K 数值评测。
6. `run_causal_workflow.py`
   - 把上面 2 到 5 步串起来，一次性跑完整流程。

---

## 4. 顶层目录说明

### `causal_ttl/`

功能：项目核心代码都在这里，包含数据结构、frontdoor 参考实现、teacher、监督导出、训练、推理、评测等。

当前问题：这个目录内部已经明显分层，但历史兼容层还在，导致“真正实现在哪个子包里”和“顶层 shim 只是转发”这件事对新读者不够直观。

### `configs/`

功能：保存 mock 实验和当前主流程的配置文件。

当前问题：mock 配置和真实训练配置混在一起，文件命名虽然能区分，但第一次接手时还是容易误用。

### `data/`

功能：保存整理后的标准化数据集。

当前问题：当前项目默认假设数据已经被整理成固定格式，缺少更强的输入校验，也缺少更多格式支持。

### `local_data/`

功能：本地临时数据目录。

当前问题：这不是当前主脚本强依赖的目录，更像工作痕迹目录，容易让人误以为它是正式输入目录。

### `local_runs/`

功能：本地临时实验目录。

当前问题：和 `outputs/` 功能接近，但没有统一规范，容易分散实验结果。

### `outputs/`

功能：保存导出的监督数据、teacher cache、模型输出、预测结果、评测结果等。

当前问题：内容很容易越积越多，尤其是 `teacher_cache` 会缓存错误响应；如果不手动清理，旧缓存会污染新实验。

### `scripts/`

功能：保存 bootstrap 和旧 dry-run 辅助脚本。

当前问题：这里的脚本主要服务于旧的 mock / autodl 启动线，不是当前 GSM8K 主流程的唯一入口。

### `.env` 与 `.env.example`

功能：保存 teacher API、模型名等环境变量模板。

当前问题：代码只会自动读取项目根目录下的 `.env`，不会自动读取上级目录的 `.env`，这一点以前已经造成过远端运行时的困惑。

---

## 5. 顶层脚本模块详解

### `prepare_dataset.py`

功能：把一个原始 CSV/JSON 数据集，或者显式的 train/test 输入，整理成统一的 `train.json / valid.json / test.json / manifest.json`。

输入：`--input` 或 `--train-input + --test-input`。

输出：标准化后的 JSON 文件，每条记录统一成 `id / question / answer / knowledge / source / split`。

当前问题：

1. 切分方式只是随机切分，没有做分层采样，也没有做重复样本检查。
2. 没有对数据集字段质量做更强的校验，例如空答案、重复 question、异常长文本。
3. 这是“准备数据”的脚本，不负责验证后续 teacher 是否适合该数据。

### `prepare_gsm8k_subset.py`

功能：从 GSM8K 风格的大集合里抽取固定数量的训练集和测试集，写入 `data/processed/gsm8k_500/`。

输入：一个 GSM8K 风格 JSON 文件。

输出：`train.json / valid.json / test.json / manifest.json`。

当前问题：

1. `--limit` 参数目前基本没有真正参与主逻辑，脚本真正使用的是 `train_size + test_size`。
2. `valid.json` 永远写成空列表，这对简单实验没问题，但不利于更正式的调参流程。
3. 默认源路径依赖外部兄弟目录 `CloudDeviceTTL`，因此它不是完全自包含的。

### `export_supervision.py`

功能：这是当前主链路的第一关键入口。它读取数据，构建 teacher，选择 `fact / v6 / v7` 模式，可选运行 frontdoor 分析，然后导出监督数据。

输入：标准化数据集、teacher 配置、导出模式、frontdoor 参数。

输出：

1. `supervision_dataset.jsonl`
2. `answer_records.jsonl`
3. `counterfactual_records.jsonl`
4. `policy_records.jsonl`
5. `teacher_payloads.jsonl`
6. `frontdoor_traces.jsonl`
7. `supervision_summary.json`

当前问题：

1. 默认 `teacher-backend` 是 `api`，如果 `.env` 没配好，脚本会直接失败。
2. teacher 输出结构不稳定时，这里会成为第一个故障点。
3. frontdoor 开启后，每条样本还会额外跑一遍 mock 前门流程，速度会明显变慢。
4. 当前没有内建“跳过坏样本继续导出”的恢复机制，出错往往会中断整次导出。

### `train_causal_ttl.py`

功能：读取监督数据，构建 `TrainingRuntimeConfig`，然后调用训练子包完成 LoRA 或 full fine-tuning。

输入：`supervision_dataset.jsonl`，基础模型路径，训练超参。

输出：训练后的模型目录和 `training_summary.json`。

当前问题：

1. 当前默认设计更偏向 7B 级别 LoRA 单卡实验，不是大规模训练系统。
2. 缺少更细粒度的恢复训练、断点续训说明。
3. 对长 prompt 的处理是静默截断，超长样本可能丢失上下文。

### `predict_causal_ttl.py`

功能：加载模型和可选 LoRA adapter，对测试数据执行推理。

输入：测试集、基础模型、可选 adapter、teacher 配置。

输出：`predictions.jsonl` 和对应 summary。

当前问题：

1. `v7` 模式会强依赖 teacher，因为要先生成 teacher payload 才能构造 prompt。
2. 这意味着即便本地模型本身可推理，只要 teacher API 挂了，`v7` 也会失败。
3. 当前版本里 `v7` prompt 很简化，导致真正的 TTL 交互式推理没有稳定发生。

### `evaluate_causal_ttl.py`

功能：读取预测文件，按 `text` 或 `gsm8k` 指标做离线评测。

输入：`predictions.jsonl`。

输出：评测 summary 和 `*_evaluation.json`。

当前问题：

1. 目前指标比较简单，主要是文本相似度和 GSM8K 数值精确匹配。
2. 还没有专门评估 `ASK_CLOUD` 使用质量、policy 正确性、counterfactual 一致性。

### `run_causal_workflow.py`

功能：把导出、训练、预测、评测串成一个配置驱动的全流程脚本。

输入：一个 JSON 或 YAML 配置。

输出：监督数据、模型、预测文件、评测结果、`workflow_summary.json`。

当前问题：

1. 一旦中间某一步失败，整条流水线通常要重新跑。
2. 没有 stage skipping，也没有 resume checkpoint 机制。
3. 如果 teacher API 不稳定，这个脚本会变得比较脆弱。

### `experiment_runner.py`

功能：这是旧 mock 线的主入口。它读取 mock 配置，构建 baseline，然后跑 `CausalTTLPipeline`。

输入：mock 配置和数据。

输出：`report.json` 和 `predictions.csv`。

当前问题：

1. 这不是当前 GSM8K 主实验线的核心入口。
2. 它依赖 mock baseline，容易让新读代码的人误会它就是正式训练管线。

### `baselines.py`

功能：定义 mock baseline 注册表，目前包括 `static_mock` 和 `causal_ttl_mock`。

输入：样本列表和 `PipelineConfig`。

输出：`SampleRunResult` 列表。

当前问题：

1. 这里只是示意性 baseline，不是论文级别对比方法集合。
2. 和当前主训练链路之间没有直接复用关系。

### `metrics.py`

功能：汇总 mock 流水线的结果，写 JSON 和 CSV 报告。

输入：`SampleRunResult`。

输出：summary、flatten 后的 CSV、JSON 报告。

当前问题：

1. 主要服务于 mock pipeline，不服务于当前主训练链路。
2. 指标设计偏展示和追踪，不偏严格 benchmark。

### `run_demo.py`

功能：最简单的前门流程演示入口，直接跑 `CausalTTLPipeline`，打印每条样本的 baseline / final / diagnosis。

输入：数据集和前门参数。

输出：控制台摘要，可选 JSON。

当前问题：

1. 它更像演示程序，不是主实验管线。
2. 仍然依赖 mock 组件。

### `README.md`

功能：项目说明入口，包含两套工作流的介绍。

当前问题：

1. README 仍然混合了早期 scaffold 和后期 GSM8K 迁移内容。
2. 内容真实但不够聚焦，容易让读者不知道“当前主流程”是哪一条。

### `requirements.txt`

功能：声明训练、推理、数据处理所需依赖。

当前问题：

1. 依赖只说明了 Python 包，没有锁版本，也没有环境导出文件。
2. 不同机器上安装结果可能略有差异。

### `.env.example`

功能：给 teacher API、student model 等环境变量提供模板。

当前问题：

1. 模板是对的，但项目代码不会自动从“项目外部目录”找 `.env`。
2. 这件事在远端跑实验时已经多次造成混淆。

---

## 6. `causal_ttl` 支撑层模块详解

### `causal_ttl/__init__.py`

功能：统一对外导出最常用的接口，方便顶层脚本直接 `from causal_ttl import ...`。

当前问题：

1. 导出项较多，学习成本不低。
2. 需要靠文档说明“真正实现在哪个子包里”，否则只看 `__init__` 不容易定位源头。

### `causal_ttl/schema.py`

功能：定义整个项目的基础数据结构，包括 `MedSample`、`ReasoningPath`、`ReasoningCluster`、`Diagnosis`、`InterventionResult`、`SampleRunResult`。

作用：这是整个项目的数据协议层。几乎所有模块都依赖这里的 dataclass。

当前问题：

1. 数据结构本身设计清晰，但还没有更严格的校验层。
2. 没有版本化 schema；如果以后字段变化，历史产物兼容性需要手动维护。

### `causal_ttl/config.py`

功能：定义 `PipelineConfig` 和 `FrontDoorConfig`。

作用：分别控制 mock 前门流程本身，以及监督导出时 frontdoor 分析相关的参数。

当前问题：

1. 参数含义比较多，但没有更系统的配置校验。
2. frontdoor 参数和真实训练参数分布在不同文件里，新人容易一开始不知道该改哪一层。

### `causal_ttl/data.py`

功能：统一读取 CSV、instruction JSON、prepared JSON、JSONL，并把它们转换成 `MedSample` 或字典列表。

作用：这是数据入口适配层。

当前问题：

1. 当前只支持 `.csv` 和 `.json` 两类样本输入，不直接支持 `.jsonl` 数据集输入。
2. 不做太强的字段质量检查。
3. 里面混合了英文 instruction 数据和中文/医学数据的兼容逻辑，后续继续扩展会越来越重。

### `causal_ttl/text_utils.py`

功能：提供文本标准化、token 化、加权 Jaccard、句子切分、简单 answer score、干扰答案生成。

作用：给 mock 推理、聚类、评测提供轻量级文本工具。

当前问题：

1. 这个文件存在明显的编码损坏痕迹，中文常量已经出现乱码。
2. 干扰答案是硬编码的中文医学短语，对 GSM8K 这种算术数据并不自然。
3. `answer_score` 是轻量启发式，不适合替代正式评测。

### `causal_ttl/knowledge_tokens.py`

功能：定义 `<knowledge>` 特殊标记及其填充、替换、注册逻辑。

作用：这是 teacher 输出、推理交互、特殊 token 注册的共同基础。

当前问题：

1. 这套机制本身是必要的，但目前只在部分路径中真正发挥作用。
2. 如果推理 prompt 根本不触发 `<knowledge>` 或 `ASK_CLOUD`，这个模块的价值就体现不出来。

### `causal_ttl/causal_path.py`

功能：把 teacher payload 规范化成统一的 steps，并格式化成 `Step / Action / Query / Observation / Causal / Need` 结构。

作用：这是 `v7` 风格监督的重要中间层。

当前问题：

1. 它能很好地把监督数据格式化出来，但当前稳定分支的推理 prompt 并没有充分利用这些结构。
2. `ensure_cloud_step()` 会在没有 ASK_CLOUD 但有 facts 的情况下强行补一个 cloud step，这对训练有帮助，但也可能和 teacher 原意不完全一致。

### `causal_ttl/evaluation.py`

功能：对预测结果做自动指标选择、GSM8K 数值提取、精确匹配统计、文本相似度统计。

作用：这是当前主链路的评测核心。

当前问题：

1. 评测维度还比较窄。
2. 只能回答“答对了多少”，还不能回答“TTL 为什么有效或无效”。

### `causal_ttl/clustering.py`

功能：这是一个兼容层，把聚类接口转发到 `frontdoor/clustering.py`。

当前问题：

1. 文件本身没错，但它只是 shim，读者容易误以为这里有独立实现。

---

## 7. `causal_ttl/frontdoor` 模块详解

这一组文件主要服务于“PPT 方法参考实现”和“导出阶段的样本加权”。

### `frontdoor/interfaces.py`

功能：定义 `EdgeReasoner` 和 `CloudDiagnoser` 两个抽象接口。

作用：这是 Stage 1 和 Stage 2 的抽象边界。

当前问题：

1. 抽象层是对的，但项目当前仍主要用 mock / heuristic 实现，没有真正的模型实现类。

### `frontdoor/reasoners.py`

功能：定义 `MemoryRule` 和 `MockEdgeReasoner`。

作用：模拟边侧 SLM 的三件事：

1. 生成多条 reasoning paths。
2. 在 diagnosis 作用下修正答案。
3. 吸收反馈到一个简化记忆库。

当前问题：

1. 这是 mock reasoner，不是真实 SLM。
2. 它直接访问 `sample.gold_answer` 来构造正确/错误路径，因此不能代表真实推理误差分布。
3. 记忆更新机制是启发式的，不是学习到的权重更新。

### `frontdoor/clustering.py`

功能：对 reasoning paths 做基于文本相似度的聚类，计算 cluster representative、weight、cohesion。

作用：对应 PPT 里的“多路径聚类保留簇中心”。

当前问题：

1. 当前聚类依赖轻量文本相似度，不是真正的表示空间聚类。
2. 对医学、数学、长推理链的泛化能力有限。

### `frontdoor/diagnosers.py`

功能：定义 `HeuristicCloudDiagnoser`。

作用：模拟云侧 LLM 对 cluster representative 给出错误类型、关键因子、修正规则、正确答案。

当前问题：

1. 这是 heuristic，不是真实 LLM。
2. 它会直接使用 `sample.gold_answer` 作为 corrected answer，这意味着这是离线 oracle 逻辑，不是线上可部署逻辑。
3. 它适合作为方法占位，不适合作为最终实验结论来源。

### `frontdoor/interventions.py`

功能：对每个 cluster 构造 treated / null / swapped / corrupted 诊断，并比较它们带来的得分变化，计算 `causal_gain`、`weighted_gain`、`ttl_loss`。

作用：这是 Stage 3 最接近“因果干预”表达的代码。

当前问题：

1. 干预对象仍然是 mock diagnosis，不是真实 teacher 诊断。
2. `swapped` 和 `corrupted` 是人工控制组，不是真实世界的因果对照。
3. 更适合离线样本加权，不适合作为部署期在线策略本体。

### `frontdoor/workflow.py`

功能：把 `generate_reasoning_paths -> cluster -> diagnose -> evaluate_interventions -> revise_answer` 串成完整流程。

作用：这是 mock 参考线的总控。

当前问题：

1. 当前 pipeline 的所有收益都建立在 mock edge 和 heuristic cloud 上。
2. 它对理解方法很有帮助，但不能直接证明接入真实模型后也会有效。

---

## 8. `causal_ttl/teachers` 模块详解

这一组文件是当前主链路里最关键、也是最脆弱的一层。

### `teachers/cache.py`

功能：以 `sha256(prompt + system_prompt + metadata)` 为键，把 teacher 响应缓存到 JSON 文件。

作用：减少 API 调用量，加快重复实验。

当前问题：

1. 缓存会把“错误响应”也一起缓存下来。
2. 如果不主动清理 cache，排障时很容易误以为代码没生效。

### `teachers/factory.py`

功能：自动读取项目根目录 `.env`，根据 `heuristic` 或 `api` 选择 teacher 实现。

作用：统一 teacher 构建入口。

当前问题：

1. 只会自动读取“项目根目录”的 `.env`。
2. 如果用户把 `.env` 放在别的目录，需要手动 `source`。
3. 以前已经因此出现过“明明有 `.env` 但程序说缺少 API base”的问题。

### `teachers/base.py`

功能：定义 `BaseTeacher` 抽象类，并实现：

1. `generate_causal()`
2. `generate_causal_sample()`
3. `fill_knowledge()`

作用：这是 teacher 的统一行为规范。

当前问题：

1. 当前的 repair 逻辑只有“一次原始生成 + 一次修复生成”的两轮策略，鲁棒性还不够强。
2. 如果 teacher 连修复也输出得很差，这里没有更高级的投票或 fallback 机制。

### `teachers/heuristic.py`

功能：提供一个不依赖 API 的本地 heuristic teacher，用于 dry-run。

作用：让工程链路在没有真实 teacher 的时候也能跑起来。

当前问题：

1. 这是明显的占位实现。
2. 它直接从 `sample.knowledge` 和 `sample.gold_answer` 取信息，不具备真实 teacher 的错误模式。
3. 只适合测试工程，不适合做实验结论。

### `teachers/openai_api.py`

功能：封装 OpenAI-compatible `/chat/completions` 请求。

作用：这是当前 teacher API 的正式实现。

当前问题：

1. 这层对 `.env` 内容非常敏感，之前已经出现过：
   - `UnicodeEncodeError`
   - `401 Authorization Required`
2. 它是同步 `urllib` 调用，没有流式、没有并发队列、没有更丰富的限流处理。
3. 它只支持 OpenAI-compatible 接口，不支持别的 provider SDK。
4. 它确实做了 `content / reasoning_content / JSON object` 的抽取，但 provider 一旦输出风格变化，仍然可能出问题。

### `teachers/parsing.py`

功能：这是当前 teacher 层最复杂的文件。它负责：

1. 定义 teacher system prompt
2. 定义 arithmetic/GSM8K 专用 prompt
3. 定义 repair prompt
4. 抽取 JSON block
5. 恢复 section 结构
6. 从 prose 中恢复 arithmetic steps / facts / answer
7. 对候选 payload 打分
8. 判断是否需要 repair

作用：它的存在，是因为真实 teacher 输出经常不稳定。

当前问题：

1. 这个文件已经很强，但也已经很脆。
2. 复杂度高，维护成本高。
3. 一旦后续接入的新模型输出风格变化，这里可能又要继续加规则。
4. 它本质上是在为“teacher 输出不稳定”兜底，而不是从根本上解决 teacher 输出不稳定。

---

## 9. `causal_ttl/exports` 模块详解

### `exports/config.py`

功能：定义 `SupervisionExportConfig`，以及 `fact / v6 / v7` 三种模式到参数配置的映射。

作用：让导出逻辑用统一配置描述。

当前问题：

1. 这三种模式语义依赖项目背景知识，第一次接手的人不容易立刻理解差别。
2. 没有更强的“模式说明文档到参数”的自动对照。

### `exports/frontdoor.py`

功能：把 mock 前门流程嵌入监督导出阶段，用来生成：

1. frontdoor summary
2. frontdoor trace
3. sample weight

作用：这是 mock 参考线和当前主训练线之间的桥梁。

当前问题：

1. 这里使用的前门分析仍然依赖 mock edge reasoner 和 heuristic diagnoser。
2. 它本质上是“离线权重生成器”，不是线上实时 teacher。
3. 权重质量是否真的能带来训练收益，目前还没有被充分证明。

### `exports/records.py`

功能：把 teacher payload 和 frontdoor summary 组装成监督样本。

它会生成三类样本：

1. `answer`
2. `counterfactual`
3. `policy`

作用：这是整个监督数据构造的核心。

当前问题：

1. `answer` 类样本的 target 最终使用的是 `sample.gold_answer`，这更像 supervised fine-tuning，而不完全是 teacher-only distillation。
2. 如果 teacher payload 本身很差，这里只能做一定程度补救，不能凭空补出高质量 supervision。
3. fallback counterfactual 比较模板化，质量可能不高。

### `exports/runner.py`

功能：调用 `build_supervision_records()`，并把所有 JSONL 和 summary 落盘。

作用：这是导出子包的最终写盘层。

当前问题：

1. 功能明确、问题不大。
2. 真正的风险主要来自上游 teacher 与 records 构造。

---

## 10. `causal_ttl/training` 模块详解

### `training/config.py`

功能：定义训练运行时参数 `TrainingRuntimeConfig`。

作用：统一承载模型路径、LoRA 参数、batch size、loss 权重等。

当前问题：

1. 参数足够当前实验使用，但还没有更成熟的验证和默认策略。
2. 当前默认更偏向单卡 7B LoRA，不是通用大训练框架。

### `training/dataset.py`

功能：读取监督 records，并把 `prompt + target` token 化成训练样本。

作用：这是数据到 token 的桥梁。

当前问题：

1. 超长 prompt 会被静默截断。
2. 当前没有额外的 curriculum、packing、length bucketing。
3. 如果 supervision 质量差，这层不会做质量过滤。

### `training/runtime.py`

功能：决定 `torch_dtype`，并根据配置挂 LoRA。

作用：这是训练时模型装配层。

当前问题：

1. LoRA target 默认只配了 `q_proj,v_proj`，对不同模型不一定最佳。
2. 没有提供 4bit/8bit 量化训练等更多显存优化选项。

### `training/trainer.py`

功能：提供自定义 data collator 和 weighted trainer。

作用：实现：

1. 按 `answer / counterfactual / policy` 设置 task-level loss weight
2. 按 frontdoor summary 设置 sample-level weight

当前问题：

1. 这是当前 TTL 训练最关键的实现之一，但它只做“加权 CE”，还没有更复杂的专用 objective。
2. 没有单独输出每个 task_type 的分项指标。

### `training/runner.py`

功能：加载 tokenizer / model，注册特殊 token，挂 LoRA，构建 HF Trainer，训练并保存 summary。

作用：这是训练子包总控。

当前问题：

1. 仍然是单机、单进程导向。
2. 没有更强的 checkpoint 恢复与断点管理。
3. 性能和稳定性仍然高度依赖底层 HF/PEFT 版本兼容性。

---

## 11. `causal_ttl/inference` 模块详解

### `inference/config.py`

功能：定义 `InferenceRuntimeConfig`。

作用：统一承载推理时的模型路径、adapter、mode、chunk_size、cloud_budget_ratio 等参数。

当前问题：

1. 参数已经够用，但策略层仍然比较薄。

### `inference/runtime.py`

功能：加载推理模型和 tokenizer，注册特殊 token，按需挂 LoRA adapter，并提供 `allow_cloud_call()`。

作用：这是推理装配层。

当前问题：

1. 没有量化推理选项，显存需求仍然不低。
2. `allow_cloud_call()` 只按哈希比例做预算控制，不考虑问题难度或模型不确定性。

### `inference/prompts.py`

功能：根据 `fact / v6 / v7` 模式构造推理 prompt。

作用：这是推理时 teacher payload 到最终 prompt 的转换层。

当前问题：

1. 当前稳定版本里，`v7` prompt 只有：
   - `Question: ...`
   - `[Reasoning]`
2. 它没有把 teacher 的 `steps / facts / diagnostics` 真正用进去。
3. 这直接导致 `v7` 很难稳定触发真正的 `ASK_CLOUD` 行为。

### `inference/generate.py`

功能：包含：

1. `extract_final_answer()`
2. `generate_direct()`
3. `interactive_generate()`
4. 与 `ASK_CLOUD`、`<knowledge>` 相关的规则识别

作用：这是当前推理交互的核心实现。

当前问题：

1. 它当前要求模型“自己先生成出 `ASK_CLOUD` 或 `<knowledge>`”，然后代码才会接管云调用。
2. 但当前 `v7` prompt 不够强，模型经常不会主动生成这种结构。
3. 这也是此前多次实验里 `avg_cloud_calls = 0.0` 的根本原因之一。
4. 这个文件里还残留了编码损坏的中文不确定性关键词，说明曾经发生过编码污染。

### `inference/runner.py`

功能：把样本逐条送入推理模型，构造 prompt，执行 direct 或 interactive inference，然后写出 prediction rows。

作用：这是推理子包总控。

当前问题：

1. 在 `v7` 模式下，即使只是想跑本地模型，也必须先有 teacher。
2. 这让 API teacher 成为推理阶段的单点故障。
3. 因为 prompt 本身较弱，当前 `interactive_generate()` 很多时候并没有真正体现 TTL 交互的优势。

---

## 12. 兼容层模块详解

下面这些文件大多不是“新实现”，而是为了兼容旧导入路径保留下来的转发层。

### `causal_ttl/pipeline.py`

功能：把 `CausalTTLPipeline` 从 `frontdoor` 子包重新导出。

当前问题：容易让人误以为这里有独立实现。

### `causal_ttl/teacher.py`

功能：把 teacher 相关接口从 `teachers` 子包重新导出。

当前问题：会掩盖真正实现位于 `teachers/`。

### `causal_ttl/supervision.py`

功能：把监督导出接口从 `exports` 子包重新导出。

当前问题：只是 shim，不是核心逻辑所在。

### `causal_ttl/ttl.py`

功能：把 `evaluate_interventions` 从 frontdoor 层重新导出。

当前问题：文件名容易让人以为“这里就是 TTL 训练主实现”，其实不是。

### `causal_ttl/hf_training.py`

功能：把训练相关接口从 `training/` 子包重新导出。

当前问题：只是旧入口兼容层。

### `causal_ttl/hf_inference.py`

功能：把推理相关接口从 `inference/` 子包重新导出。

当前问题：只是旧入口兼容层。

### `causal_ttl/adapters.py`

功能：把 frontdoor 中的接口和 mock 实现重新导出。

当前问题：命名容易让人误解为“真实模型 adapter 层”，实际上目前仍偏 mock。

### `causal_ttl/clustering.py`

功能：把 frontdoor 聚类函数重新导出。

当前问题：只是兼容层。

---

## 13. 配置、脚本与产物目录说明

### `configs/qa_mock.yaml` 与 `configs/digestive_mock.yaml`

功能：服务于旧 mock / dry-run 线。

当前问题：不应和当前 GSM8K 主实验线混为一谈。

### `configs/causal_ttl_v7_workflow.yaml`

功能：本地完整工作流示例配置。

当前问题：可以跑，但它更像通用示例，不是专门为 GSM8K 调过的最精简版本。

### `configs/causal_ttl_fact_workflow.yaml`

功能：`fact` 模式 workflow 配置。

当前问题：主要用于模式验证，不是当前最主要实验配置。

### `configs/causal_ttl_gsm8k_v7_workflow.yaml`

功能：当前最贴近 GSM8K 实验的 workflow 配置。

当前问题：

1. 它能表达主实验链路，但 teacher API 是否可用仍然依赖环境。
2. workflow 一旦失败，没有内建 resume。

### `scripts/bootstrap_autodl.sh`

功能：在云端快速装依赖并创建基本目录。

当前问题：非常轻量，不会替你检查 CUDA、driver、模型文件等更复杂条件。

### `scripts/run_autodl_experiment.sh`

功能：调用 `experiment_runner.py` 跑旧 mock 线。

当前问题：它不是当前主实验线脚本。

### `scripts/run_local_dry_run.ps1`

功能：Windows 本地启动 mock dry-run。

当前问题：路径里已有编码损坏痕迹，说明脚本本身也受过编码环境影响。

---

## 14. 当前实验状态与已经观察到的问题

### 14.1 已经观察到的成功点

1. 项目在当前稳定分支上，曾经成功跑通过 `10` 条 GSM8K 的完整流程。
2. 说明：
   - 数据准备链路是通的。
   - 导出链路是通的。
   - 训练链路是通的。
   - 推理和评测链路也是通的。

### 14.2 已经观察到的失败点

1. teacher API 曾经出现过 `UnicodeEncodeError`，根因是 `.env` 或传入 key 中混入了非 ASCII 内容。
2. teacher API 也出现过 `401 Authorization Required`，根因是 key 无效、未正确加载或 provider 鉴权失败。
3. teacher 输出经常不稳定，所以 `teachers/parsing.py` 被迫越来越复杂。
4. 在后续更激进的改动尝试里，`v7` 推理端虽然跑了，但多次出现 `avg_cloud_calls = 0.0`。
5. 在已经做过的一个 `10` 条小实验里，base model 表现优于 TTL model，这说明“方法闭环能跑”不等于“方法已经有效”。

### 14.3 目前最重要的工程问题

1. teacher 是单点故障。
2. teacher cache 会污染复现实验。
3. 没有断点续跑。
4. 没有自动测试。
5. 编码环境曾经污染过若干文件。

---

## 15. 当前最重要的问题清单

### 问题 1：teacher 质量和 teacher 稳定性仍然是全局瓶颈

影响：

1. 导出会失败。
2. 推理会失败。
3. 导出的 supervision 质量会抖动。
4. 实验结论会不稳定。

### 问题 2：`v7` 推理机制在当前稳定分支里没有真正发挥出来

影响：

1. 云调用触发不充分。
2. policy supervision 学到的内容很难在推理时被真正使用。
3. TTL 的方法优势没有被完整测试到。

### 问题 3：frontdoor 目前更像离线加权器，不是真正的在线可部署算法

影响：

1. 它适合做“训练样本重要性估计”。
2. 它还不适合被描述成“已经完成真实模型版 Stage 1 和 Stage 2”。

### 问题 4：仓库存在双主线，学习成本偏高

影响：

1. 新人容易跑错脚本。
2. 容易误把 mock 线当成主实验线。
3. 文档如果不明确，后续继续迭代会越来越混乱。

### 问题 5：编码污染已经影响了部分文件

已经看到的迹象：

1. 旧版 `当前说明.md` 已经乱码。
2. `text_utils.py` 中的中文常量已损坏。
3. 部分 PowerShell 路径字符串也有乱码痕迹。

影响：

1. 中文数据实验的可读性和可维护性下降。
2. 某些启发式逻辑可能会被暗中影响。

---

## 16. 后续建议

### 第一优先级

1. 先把 teacher API 稳定下来。
2. 先在当前稳定版本上重新确认 `10` 条 GSM8K 能稳定复现。
3. 每次重跑前确认是否要清 teacher cache。

### 第二优先级

1. 修复 `v7` 推理 prompt 与 teacher scaffold 的对接。
2. 让 `ASK_CLOUD` 在推理阶段真正稳定触发。
3. 重新比较：
   - base model
   - TTL model
   - avg_cloud_calls

### 第三优先级

1. 清理编码污染。
2. 明确区分 mock 线和主实验线。
3. 给 `run_causal_workflow.py` 增加 resume / stage skip 能力。

---

## 17. 一句话总结

当前这个项目已经不是一个“空壳子”了，而是一个已经能把 `CloudDeviceTTL` 方法迁移思路落到本地工程上的实验平台。

它现在最缺的，不再是“有没有代码”，而是：

1. 更稳定的 teacher。
2. 更真实的 Stage 1 / Stage 2 模型接入。
3. 更可靠的 v7 推理交互。
4. 更明确的工程收敛与清理。

如果只问“是否已经具备继续做真实实验的基础”，答案是：**是的，已经具备。**

如果问“是否已经达到可以直接宣称方法完成、效果可信、工程成熟的程度”，答案是：**还没有，还差 teacher 稳定性、推理交互有效性、以及真实模型替换这几步。**
