# 当前说明

## 1. 项目目标

本项目的目标是把 `CloudDeviceTTL` 中可复用的因果 TTL 方法迁移到一个独立、可本地运行、便于继续扩展的工程中，并对照 PPT 中的方法定义完成核心流程落地。

当前项目关注的不是复刻 `CloudDeviceTTL` 的全部工程栈，而是完成下面三件事：

1. 把因果协同 TTL 的核心方法迁移出来。
2. 在不依赖 LLaMA-Factory 的前提下，提供可运行的导出、训练、推理、评估流程。
3. 为后续接入真实边侧小模型和云侧大模型预留清晰接口。

一句话总结当前交付状态：

`因果推断` 项目已经具备“方法完整、流程可跑、接口可替换”的完成度；剩余工作主要集中在真实模型接入，而不是方法框架本身。

## 2. 方法定义

当前项目采用的核心方法与 PPT 中的三阶段定义一致。

### Stage 1：SLM 多路径推理与聚簇

- 边侧小模型针对同一个用户 query 生成 `N` 条 CoT 路径。
- 这些路径作为小模型内部状态 `H` 的外显代理。
- 出于训练成本和统计代表性的考虑，对多条路径进行聚簇。
- 每个簇保留代表路径，并记录簇权重。

方法含义：

- 不直接观测隐藏状态 `H`，而是用多条推理路径近似表示 `H`。
- 聚簇后的代表路径用于降低后续大模型诊断成本。

### Stage 2：LLM 诊断与干预信息生成

- 云侧大模型接收聚簇后的代表性 CoT。
- 对路径进行评估，输出诊断 `C` 和更可靠的答案 `A`。
- `C` 用于描述当前路径的问题，并作为后续干预变量。

当前诊断变量 `C` 的定义包括：

- 错误类型：如 `logic`、`factual`、`omission`、`shortcut`
- 关键影响因素：即应当保留的关键医学或因果要素
- 修正规则：即从错误推理修正到正确答案的规则

方法含义：

- `C` 的作用不是简单打分，而是作为可操控、可替换、可删除的中介变量。
- `A` 提供正确目标，`C` 提供“为什么需要修正”的结构化解释。

### Stage 3：TTL loss 与因果干预验证

- 小模型接收 `C` 和 `A` 后，构造 TTL 目标。
- 把诊断 `C` 当作可干预变量，观察端侧模型答案是否按预期变化。
- 典型干预方式包括：
  - 删除诊断
  - 换成错误诊断
  - 换成其他簇的诊断

方法含义：

- 这一步不是普通蒸馏，而是通过干预验证 `C` 是否真的影响 `H -> A` 的路径。
- 因果收益越明显，对应样本在训练中应当具有更高权重。

## 3. 当前代码实现与方法的对应关系

### 3.1 Stage 1 对应实现

- `causal_ttl/frontdoor/reasoners.py`
  - `MockEdgeReasoner`
  - 负责生成多条推理路径，并模拟边侧模型状态更新
- `causal_ttl/frontdoor/clustering.py`
  - `cluster_reasoning_paths(...)`
  - 负责路径聚簇和簇权重统计
- `causal_ttl/frontdoor/workflow.py`
  - `CausalTTLPipeline.run_sample(...)`
  - 串联路径生成、聚簇、诊断和干预评估

### 3.2 Stage 2 对应实现

- `causal_ttl/frontdoor/diagnosers.py`
  - `HeuristicCloudDiagnoser`
  - 负责根据代表路径生成诊断结果
- `causal_ttl/teachers/openai_api.py`
  - `OpenAICompatibleTeacher`
  - 已提供 OpenAI-compatible 教师模型调用接口
- `causal_ttl/exports/frontdoor.py`
  - `run_frontdoor_analysis(...)`
  - 在导出监督数据时执行前门因果分析，并保留 trace

### 3.3 Stage 3 对应实现

- `causal_ttl/frontdoor/interventions.py`
  - `evaluate_interventions(...)`
  - 负责 treated / null / swapped / corrupted 等干预评估
- `causal_ttl/exports/records.py`
  - 负责把诊断结果转为 `answer`、`counterfactual`、`policy` 三类监督样本
  - 负责生成 `sample_weight`
- `causal_ttl/training/runner.py`
  - 支持 task-level loss weight
  - 支持 sample-level weight
  - 支持加权训练 summary 输出

## 4. 当前已经完成的内容

目前已经完成的部分如下。

### 4.1 方法与数据层

- 支持 `fact`、`v6`、`v7` 三种导出模式。
- 支持结构化 teacher payload：
  - `causal_factors`
  - `confounders`
  - `steps`
  - `facts`
  - `counterfactuals`
  - `answer`
- 支持 `<knowledge>` 标记和因果路径格式化。
- 支持前门分析结果写入监督数据。

### 4.2 训练层

- 支持本地 Hugging Face 因果语言模型训练。
- 支持 LoRA 微调。
- 支持三类任务联合训练：
  - `answer`
  - `counterfactual`
  - `policy`
- 支持前门分析得到的样本权重进入 loss。

### 4.3 推理与评估层

- 支持本地推理脚本。
- 支持预测结果写出为 `jsonl`。
- 支持离线评估脚本。
- 支持从配置文件一键运行完整 workflow。

### 4.4 工程层

- 已有本地工作流入口。
- 已有配置文件。
- 已有导出产物目录结构。
- 已有 smoke 级别的导出验证。
- 已完成一轮按职责分包的结构重构，当前目录已按 `frontdoor / teachers / exports / training / inference` 分层。

## 5. 当前项目与 CloudDeviceTTL 的关系

当前项目已经不是简单的草稿，而是一个独立可运行的迁移版本。

它与 `CloudDeviceTTL` 的关系可以概括为：

- 方法主线已经对齐。
- 核心样本构造逻辑已经对齐到 `fact / v6 / v7` 语义。
- 训练目标中 answer / counterfactual / policy 三类 supervision 已具备。
- 前门分析已经接入到监督导出和样本加权。

但它还不是对 `CloudDeviceTTL` 的完全等价复刻，原因主要有两点：

1. 当前前门流程中的边侧推理器仍然是 `MockEdgeReasoner`。
2. 当前前门流程中的云侧诊断器仍然是 `HeuristicCloudDiagnoser`。

因此，更准确的表述是：

- 在“方法迁移完成度”上，已经接近目标。
- 在“真实模型工程化完成度”上，还差真实 SLM / LLM 接入。

## 6. 当前完成度结论

如果问题是：

“我的方法部分是不是已经基本完成了？”

答案是：

是的，方法框架、数据流、训练流和评估流已经基本完成，你负责的因果方法迁移部分可以认为已经交付。

如果问题是：

“是不是现在只差填一个模型名就完全结束？”

答案是：

还不能这么说。当前更准确的状态是：

- 主流程已经达到“接入真实模型即可开始实验”的完成度。
- 但若要宣称完全达到 `CloudDeviceTTL` 的工程成熟度，还需要把前门流程中的 mock 组件替换成真实模型组件。

## 7. 剩余工作边界

后续剩余工作主要集中在模型接入，不再是方法补洞。

### 7.1 必做项

1. 把 `MockEdgeReasoner` 替换为真实边侧小模型接口。
2. 把 `HeuristicCloudDiagnoser` 替换为真实云侧大模型诊断接口。
3. 使用真实模型完整跑通一次：
   - 监督导出
   - 训练
   - 推理
   - 评估

### 7.2 可选增强项

1. 把前门分析从“导出时离线分析”进一步发展为“推理时在线干预”。
2. 增强诊断 `C` 的结构化格式和可解释性。
3. 增强更多和 `CloudDeviceTTL` 原工程一致的交互式工作流。

## 8. 关键文件说明

下面这些文件是当前项目最关键的入口。

- `causal_ttl/frontdoor/workflow.py`
  - 前门分析主流程
- `causal_ttl/frontdoor/reasoners.py`
  - 边侧推理器默认实现
- `causal_ttl/frontdoor/diagnosers.py`
  - 云侧诊断器默认实现
- `causal_ttl/frontdoor/interventions.py`
  - 干预评估与 TTL 相关逻辑
- `causal_ttl/teachers/`
  - teacher 相关实现
  - 包括 `base.py`、`heuristic.py`、`openai_api.py`、`factory.py`
- `causal_ttl/exports/`
  - supervision 导出相关实现
  - 包括 `config.py`、`frontdoor.py`、`records.py`、`runner.py`
- `causal_ttl/training/`
  - 加权训练相关实现
  - 包括 `config.py`、`dataset.py`、`trainer.py`、`runner.py`
- `causal_ttl/inference/`
  - 本地推理相关实现
  - 包括 `config.py`、`prompts.py`、`generate.py`、`runner.py`
- `export_supervision.py`
  - 导出 supervision 数据
- `train_causal_ttl.py`
  - 训练入口
- `predict_causal_ttl.py`
  - 推理入口
- `evaluate_causal_ttl.py`
  - 评估入口
- `run_causal_workflow.py`
  - 一键 workflow 入口

补充说明：

- `causal_ttl/adapters.py`
- `causal_ttl/pipeline.py`
- `causal_ttl/supervision.py`
- `causal_ttl/ttl.py`
- `causal_ttl/teacher.py`
- `causal_ttl/hf_training.py`
- `causal_ttl/hf_inference.py`

这些顶层文件目前仍然保留，但主要作用已经变成兼容入口，用于保证旧脚本和旧导入路径不失效。

## 9. 典型运行方式

建议先进入项目目录：

```powershell
cd .\因果推断
```

### 9.1 导出监督数据

使用启发式 teacher 本地导出：

```powershell
python .\export_supervision.py `
  --dataset ..\MedThink\PrecisionBoost\q&a.csv `
  --mode v7 `
  --teacher-backend heuristic `
  --enable-frontdoor `
  --output-dir .\outputs\supervision_v7_frontdoor
```

### 9.2 训练本地模型

```powershell
python .\train_causal_ttl.py `
  --train-records .\outputs\supervision_v7_frontdoor\supervision_dataset.jsonl `
  --model-name-or-path Qwen/Qwen2.5-7B-Instruct `
  --output-dir .\outputs\model_v7 `
  --task-types answer counterfactual policy `
  --answer-loss-weight 1.0 `
  --counterfactual-loss-weight 1.0 `
  --policy-loss-weight 1.0
```

### 9.3 本地推理

```powershell
python .\predict_causal_ttl.py `
  --dataset .\data\processed\qa_test\test.json `
  --model-name-or-path Qwen/Qwen2.5-7B-Instruct `
  --adapter-path .\outputs\model_v7 `
  --mode v7 `
  --predictions-path .\outputs\model_v7\predictions.jsonl
```

### 9.4 结果评估

```powershell
python .\evaluate_causal_ttl.py `
  --predictions .\outputs\model_v7\predictions.jsonl
```

### 9.5 一键运行完整 workflow

```powershell
python .\run_causal_workflow.py `
  --config .\configs\causal_ttl_v7_workflow.yaml
```

## 10. 主要输出产物

监督导出阶段的主要产物包括：

- `supervision_dataset.jsonl`
- `answer_records.jsonl`
- `counterfactual_records.jsonl`
- `policy_records.jsonl`
- `teacher_payloads.jsonl`
- `supervision_summary.json`
- `frontdoor_traces.jsonl`

训练阶段的主要产物包括：

- 模型或 LoRA adapter 输出目录
- `training_summary.json`

推理阶段的主要产物包括：

- `predictions.jsonl`

完整 workflow 的主要产物包括：

- `workflow_summary.json`

## 11. 真实模型接入说明

本项目后续需要两类外部模型。

### 11.1 边侧模型

作用：

- 作为 student / SLM
- 负责训练与推理
- 在更完整版本中，也应承担 Stage 1 的真实多路径生成

当前接入位置：

- `train_causal_ttl.py`
- `predict_causal_ttl.py`
- `run_causal_workflow.py`
- `causal_ttl/training/`
- `causal_ttl/inference/`

目前训练与推理已经可以直接指定：

- `--model-name-or-path`

### 11.2 云侧模型

作用：

- 作为 teacher / LLM
- 输出结构化因果解释
- 在后续真实化版本中承担 Stage 2 诊断

当前接入方式：

- `teacher-backend api`
- `OpenAICompatibleTeacher`
- `causal_ttl/teachers/openai_api.py`

可通过以下参数接入：

- `--teacher-api-base`
- `--teacher-api-key`
- `--teacher-model`

## 12. 当前最准确的项目结论

对于本次迁移工作，最准确的结论如下：

1. 你的方法部分已经基本完成。
2. 当前项目已经具备独立运行的导出、训练、推理、评估闭环。
3. 当前项目已经接近“接入模型即可开始真实实验”的状态。
4. 当前尚未完全达到 `CloudDeviceTTL` 的工程成熟度，主要差在真实边侧推理器和真实云侧诊断器。

因此，这个项目现在可以作为正式交接版本结束本阶段工作。
